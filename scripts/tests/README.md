# Integration Tests
Welcome to the LEGO playground.
```console
gobear  # activate virtual env
cd ~/BearCar/scripts/  # navigate to right location
```

1. driver_messenger
```console
python -m tests.driver_messenger
```

2. camera_driver_messenger
```console
python -m tests.camera_driver_messenger
```

3. camera_autopilot_messenger
```console
python -m tests.camera_autopilot_messenger
```

